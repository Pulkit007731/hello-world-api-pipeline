name: Build and Publish ASP.NET Core API

on:
  push:
    branches: [ main ]

jobs:
  build:
    runs-on: self-hosted

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      # - name: Setup .NET SDK
      #   uses: actions/setup-dotnet@v3
      #   with:
      #     dotnet-version: 9.0.x


      - name: Restore dependencies
        run: dotnet restore

      - name: Build the app
        run: dotnet build --configuration Release

      - name: Publish the app
        run: dotnet publish --configuration Release --output ./publish

      - name: Upload published files as artifact
        uses: actions/upload-artifact@v4
        with:
          name: DevopsCICD-publish
          path: ./publish

      # … everything above (checkout / restore / build / publish / upload) stays the same …

      - name: Deploy locally on EC2 runner
        shell: powershell
        run: |
          $dest = "C:\HorseDerbyAPI"

          # Create the folder if it doesn’t exist
          if (-not (Test-Path $dest)) {
            New-Item -ItemType Directory -Path $dest | Out-Null
          }


      - name: Stop IIS Website (HorseDerby)
        run: |
          Import-Module WebAdministration
          Stop-Website -Name "HorseDerby"
        shell: pwsh

      - name: Deploy to C:\HorseDerbyAPI
        run: |
          $dest = "C:\HorseDerbyAPI"
          Copy-Item -Path "$PWD\publish\*" -Destination $dest -Recurse -Force
        shell: pwsh

      - name: Start IIS Website (HorseDerby)
        run: |
          Import-Module WebAdministration
          Start-Website -Name "HorseDerby"
        shell: pwsh

          # Copy the freshly‑published files
          Copy-Item -Path "$PWD\publish\*" -Destination $dest -Recurse -Force
         

      - name: Stop IIS Website (HorseDerby)
        run: |
          Import-Module WebAdministration
          Stop-Website -Name "HorseDerby"
        shell: pwsh

      - name: Deploy to C:\HorseDerbyAPI
        run: |
          $dest = "C:\HorseDerbyAPI"
          Copy-Item -Path "$PWD\publish\*" -Destination $dest -Recurse -Force
        shell: pwsh

      - name: Start IIS Website (HorseDerby)
        run: |
          Import-Module WebAdministration
          Start-Website -Name "HorseDerby"
        shell: pwsh
          # Restart IIS so the site picks up the new build
          iisreset /restart


         
